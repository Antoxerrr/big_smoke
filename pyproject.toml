[tool.poetry]
name = "big-smoke"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
python-telegram-bot = "^22.5"
pytz = "^2025.2"
python-dotenv = "^1.2.1"
tortoise-orm = {extras = ["asyncpg"], version = "^0.25.3"}
aerich = {extras = ["toml"], version = "^0.9.2"}


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.aerich]
tortoise_orm = "bot.db.config.TORTOISE_CONFIG"
location = "./migrations"
src_folder = "./."
